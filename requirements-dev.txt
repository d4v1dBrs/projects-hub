# Dependencias de DESARROLLO — NO se instalan en el servidor (deploy.sh usa requirements.txt).
#   py -3.12 -m pip install -r requirements.lock -r requirements-dev.txt
# Este fork no tiene suite de tests (quedó en el monitor original); el único tooling es el
# linter:  py -3.12 -m ruff check .   (config en ruff.toml)
ruff==0.15.16
