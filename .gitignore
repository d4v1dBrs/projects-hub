# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.mypy_cache/
.ruff_cache/

# Logs (el log de la app va a db_dir, fuera del árbol; esto cubre restos y rotados)
*.log
*.log.[0-9]
*.log.[0-9].gz

# Artefactos generados
data/feriados_ar_cache.json

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/

# Backups manuales de Excel / data
*.bak
*.bak[0-9]

# Material de referencia BYMA (addin Excel, PDFs) — fuente local de data/byma/titulos_final.csv
BYMADATA_EXCEL/

# Artefactos de Playwright MCP (snapshots/console de pruebas de UI)
.playwright-mcp/

# Sesiones locales de brainstorming visual
.superpowers/

# Scratch local (mockups, dumps, pruebas a mano): nunca se versiona
scratch/

# Bases de datos — viven en db_dir (%LOCALAPPDATA%\monitor / ~/.local/share/monitor), nunca en el repo
*.db
*.duckdb
*.db-wal
*.db-shm
monitor/

# Secrets / credentials (NEVER commit)
.env
.env.*
!.env.example
secrets/
*.secret

# Claude Code: estado local
.claude/

# Worktrees: nunca project-local (OneDrive)
.worktrees/
worktrees/
