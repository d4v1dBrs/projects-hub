# Dependencias de RUNTIME (las instala deploy.sh en el servidor; local: py -3.12).
#   py -3.12 -m pip install -r requirements.txt        # versiones abiertas
#   py -3.12 -m pip install -r requirements.lock       # pins conocidos-buenos (local)
# Cada dependencia de acá tiene un import verificado en apps/, core/ o config/.

# --- núcleo financiero ---
numpy
scipy                    # xirr.py (Brent), yield_curve.py
pandas
python-dateutil
holidays                 # holiday_engine
pandas-market-calendars  # holiday_engine (calendario BYMA)
openpyxl                 # holiday_engine (feriados_ar.xlsx) + engine de pd.read_excel del auto-seed
websockets               # futures_provider (Matba/Rofex WS) + extra [standard] de uvicorn

# --- web / async ---
fastapi
uvicorn[standard]
httpx
# requests: ficha técnica BYMA — requests.Session en byma/catalog_enrich (la corre el
# _startup_reconcile del lifespan). Verificación TLS por core/infrastructure/_tls.py.
requests
jinja2
python-multipart         # Form() del login / ABM
sse-starlette            # /stream
SQLAlchemy>=2
pydantic>=2
pydantic-settings

# --- auth ---
passlib                  # hashing de contraseñas
bcrypt<4.0.0             # backend de passlib, fijado por un bug conocido en la lib
PyJWT                    # cookie de sesión (JWT)

# --- dev: ver requirements-dev.txt (sólo ruff; no va al servidor) ---
